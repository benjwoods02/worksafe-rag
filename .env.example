# Copy to .env and add your key. .env is gitignored.
# Retrieval needs no key -- only answer() calls the API.
# Generation costs ~$0.004/query on claude-haiku-4-5.
ANTHROPIC_API_KEY=sk-ant-your-key-here
